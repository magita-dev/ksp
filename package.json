{
  "name": "ksp-crime-ai",
  "private": true,
  "workspaces": ["frontend", "functions/orchestrator", "scripts/seed"],
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm run start",
    "lint": "cd frontend && npx next lint",
    "seed": "cd scripts/seed && npm run seed"
  },
  "dependencies": {
    "@types/nodemailer": "^8.0.1",
    "jspdf": "^4.2.1",
    "jspdf-autotable": "^5.0.8",
    "nodemailer": "^9.0.3",
    "recharts": "^3.10.1"
  }
}
